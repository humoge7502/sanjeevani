# SANJEEVANI backend (M1 + M3). All offline-capable, no service credentials.
#
# Pinned to the versions this repository was verified against.
#   pip install -r requirements.txt
#   pip install -r requirements-dev.txt   # adds pytest

fastapi==0.141.1
uvicorn==0.52.4
pydantic==2.13.5
PyYAML==6.0.3
networkx==3.6.1
